# Core dependencies for visualize_network.py
pandas
openpyxl
networkx
matplotlib

# Interactive HTML network visualization (optional, highly recommended)
pyvis

# Edge confidence modeling
scikit-learn
joblib

# Unified server dependencies (FastAPI, Uvicorn, Biosimulation)
cobra>=0.22.0
fastapi>=0.95.0
uvicorn>=0.20.0
pydantic>=2.0

# Native desktop window (replaces Chrome/Edge --app mode)
pywebview>=4.4

# Thermodynamics data builder (run offline to regenerate thermo_dgr_data.json)
equilibrator-api>=0.7.0

# Testing
pytest>=7.0
pytest-asyncio>=0.21
httpx>=0.24
